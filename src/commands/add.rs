use anyhow::Result;

pub fn run(
    _path: String,
    _machine: Option<String>,
    _platform: Option<String>,
    _commit: Option<String>,
    _dry_run: bool,
) -> Result<()> {
    todo!("add not implemented")
}
